---
name: git-worktree
description: >-
  Gitでワークツリーを作成・削除するときに使う
---

Gitのワークツリーを操作するときはgwqコマンドを使う

- ワークツリーを作成するときは `gwq add -b feature/{placeholder}`
- ワークツリーを削除するときは `gwq remove -b feature/{placeholder}`
